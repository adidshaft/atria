# Python (macOS BLE exploration tooling)
.venv/
__pycache__/
*.pyc

# Runtime artifacts from the Terminal BLE workaround
whoop_log.txt
which_script.txt
# contains a copied Python interpreter; not source
WhoopBLE.app/

# Runtime transcripts, build logs, and device/simulator debug output.
logs/

# Generated physical-device evidence can include private health/device data.
# Keep curated evidence tracked only when explicitly staged with -f.
docs/evidence/**
evidence/

# Xcode / iOS
DerivedData/
build/
*.xcuserstate
xcuserdata/
*.xcscmblueprint

# Local signing/secrets material
.env
.env.*
keys/
*.p8
*.mobileprovision

# Local execution contracts can contain device-specific history.
GOAL.md

# Local physical-device diagnostics and generated screenshots.
tmp/
artifacts/

# Local design experiments; curated public assets live in assets/.
design/

# macOS
.DS_Store
.build/

# Imported design references (local only, never committed)
atria-design-import/
design-import/
*.dc.html

# Local screenshot/scratch drop folder
/tmp/
